[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "zero-human-pr-agent"
version = "1.0.0"
description = "BTNOMB idea_006 Zero-human PR agent"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}

authors = [{name = "Interaction Company of California"}]

[tool.setuptools]
packages = ["app"]
